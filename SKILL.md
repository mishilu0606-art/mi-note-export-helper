---
name: mi-note-export-helper
description: 小米便签导出实战助手——破解 30 分钟 cookie 失效导致的"跑一半失败"，用自动续跑器把全库拉完，再整理成 Obsidian vault。当用户提到"导出小米便签"、"同步小米云笔记"、"便签导出失败"、"mi-note-export"时触发此 skill。
---

# mi-note-export-helper

从小米云导出全部便签，并整理成可直接打开的 Obsidian vault。

**本 skill 的价值集中在第一部分。** 小米便签导出真正的难点不是"怎么导"，是"**导不完**"。先解决跑不完，再谈整理。

---

## 第一部分：30 分钟失效与自动续跑（核心）

### 症状

小米登录态有效期约 **30 分钟**。跑太久就会**部分成功、部分失败**——前面的拿到了，后面的全挂。

⚠️ **最大的坑：这些失败条目极具迷惑性。** 它们看起来像是"云端已经清空的僵尸条目"，很容易误判成数据没了、或者工具坏了。实际上只是**超时失效**，数据好好地在云上。

> 判断标准：失败的条数比较大、且集中在某一轮的后半段 → 基本可以断定是超时，不是数据问题。

### 对策：反复重跑同一条命令

```bash
npx mi-note-export
```

原理：

- 每次重跑 = 重新走一遍登录流程 = **拿到新 cookie**
- 工具自带增量机制（`.sync-state.json`，基于内容哈希）
- 所以重跑**不会重复下载**，只接着上次的进度往下走
- 反复重跑，直到"待同步为零"

### 为什么能自动登录（两个条件叠加，缺一不可）

**条件一：工具用了持久化浏览器身份**

```js
chromium.launchPersistentContext(BROWSER_DATA_DIR, {...})
// 复用浏览器身份，避免每次都是新设备触发风控
```

**条件二：让浏览器保留网站数据（这一步要你手动做）**

第一次登录时，在弹出的那个浏览器窗口里，把"关闭浏览器时清除 Cookie 和网站数据"关掉，允许站点保存数据。

两个条件都满足，重跑时浏览器才会**"闪一下"就自动登录完成，不需要人工点任何东西**。

实测：千条量级的库，前后登录约 3 次即可全部拉完。

### ⚠️ 前置准备：别让浏览器清数据

这是"自动续跑"能否成立的分水岭。第一次登录时按下面做：

1. 运行 `npx mi-note-export`，会弹出一个浏览器窗口
2. 在这个窗口里完成小米账号登录
3. **设置 → 隐私和安全 → 第三方 Cookie / 网站数据**：
   - 关掉「关闭所有窗口时清除 Cookie 和网站数据」
   - 确保站点可以保存数据
4. 之后每次重跑，这个登录态都会被复用

**千万别做这两件事**（会毁掉自动登录能力，得从头再来）：

| 禁忌 | 后果 |
|---|---|
| 手动删除缓存目录下的 `browser-data`（位置见下表） | 浏览器身份丢失，小米认不出这台设备 |
| 运行 `npx mi-note-export --clear-cache` | 同上，Cookie 和浏览器数据一起被清 |

**缓存根目录（按系统）**——`cookie` 文件和 `browser-data/` 都在这里面：

| 系统 | 路径 |
|---|---|
| Windows | `%LOCALAPPDATA%\mi-note-export\cache\` |
| macOS | `~/Library/Caches/mi-note-export/` |
| Linux | `~/.cache/mi-note-export/`（或 `$XDG_CACHE_HOME/mi-note-export/`） |

> 补充：自动登录偶尔还是会失效（比如隔了很久没跑、小米侧主动踢了会话）。这时浏览器窗口会停在那里等你——**去点一下即可，脚本会一直等着**，点完继续跑。

### Agent 该怎么做（首选方案）

**直接让用户跑自动续跑器**，不要让他手动一遍遍重跑：

```powershell
.\auto-resume.ps1                      # 默认最多 10 轮
.\auto-resume.ps1 -MaxRounds 15        # 库很大时调大
.\auto-resume.ps1 -RoundTimeoutSec 900 # 网络不稳时放宽单轮超时
```

脚本行为：

| 行为 | 说明 |
|---|---|
| 循环执行 `npx mi-note-export` | 每轮 = 一次新的 cookie |
| 每轮统计笔记数量 | 打印"本轮新增 N 条，累计 M 条" |
| **连续两轮无新增 → 判定完成** | 自动停止，不用人盯 |
| **单轮看门狗 600 秒** | 卡死（网络抖动/服务器无响应）时强杀该轮进下一轮，用 `-RoundTimeoutSec` 调整 |
| 轮间等待 8 秒 | 给浏览器时间完全退出 |

若提示禁止运行脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\auto-resume.ps1
```

### 什么时候才需要人工介入

只有当**持久化身份本身也失效**时（清过缓存、换了浏览器、隔很久没跑），浏览器窗口才会停住等人。这时去点一下即可，脚本会一直等着，点完继续跑。

**保活提醒（备选）**：用户明确需要定时提醒、或不方便跑脚本时，提供 `keepalive.ps1`，每 25 分钟蜂鸣 + 弹窗一次。现在它是备选，不再是首选。

---

## 第二部分：两阶段策略

搞清哪些事受 30 分钟限制、哪些不受，能省掉大量无用功。

| 阶段 | 内容 | 是否受限 |
|---|---|---|
| **阶段一：拉数据** | 笔记列表、详情、附件下载 | ✅ 受限，依赖 cookie |
| **阶段二：本地处理** | 去重、分类、改链接、整理入库 | ❌ 不受限，纯本地 |

**策略**：登录后**趁热一口气把阶段一全部干完**（列表 + 详情 + 附件），拿到完整数据后再慢慢做阶段二。别在 cookie 有效期内去整理——那是浪费宝贵的有效期。

---

## 第三部分：导出命令

由 [mi-note-export](https://www.npmjs.com/package/mi-note-export) 完成。

**必须在用户自己的终端（cmd / PowerShell）执行**，不能在沙箱或远程环境跑——它需要弹出浏览器窗口完成登录。

```bash
npx mi-note-export            # 增量同步（自动续跑器就是反复跑这条）
npx mi-note-export --force    # 全量重来
npx mi-note-export --login    # 强制重新登录
npx mi-note-export -o <dir>   # 指定输出目录
npx playwright install chromium   # 若提示浏览器缺失
```

首次运行会下载 Playwright Chromium（约 200MB）。

### 登录与 cookie 机制（源码级）

| 环节 | 实际行为 |
|---|---|
| cookie 位置 | `<缓存根目录>/cookie`（Windows：`%LOCALAPPDATA%\mi-note-export\cache\`；macOS：`~/Library/Caches/mi-note-export/`；Linux：`~/.cache/mi-note-export/`） |
| 浏览器身份 | `<缓存根目录>/browser-data`（持久化，自动登录靠它） |
| 验证时机 | **只在进程启动时验证一次** |
| 失效后 | 打印「⚠️ Cookie 已过期」→ 打开 Chrome 让你登录 |
| **关键缺陷** | 跑起来之后 cookie 过期**不会自动续**——这就是必须反复重跑的根因 |

### 输出结构

```
<output>/
├── assets/                    # 附件，命名 type_date_hash.ext
├── .sync-state.json           # 增量状态，续跑靠它；内含每条的 modifyDate（organize.py 去重会用）
├── 文件夹名/笔记标题.md
└── 笔记标题.md                 # 或 2025-01-01_12-00-00.md（无标题）
```

### 已知限制

- 大批量失败时，**先按 cookie 过期处理**（反复重跑），这是最常见的原因；
- 私密便签、待办、脑图等特殊类型**可能**导不出（不同版本工具支持程度不同）——
  重跑多轮后仍固定失败的那一小批，再按类型问题对待，别提前下结论；
- 导出失败 ≠ 数据丢失：失败的条目还在小米云端和手机上。
- 空笔记（无标题无内容）自动跳过

---

## 第四部分：整理（阶段二）

导出完整后才做这一步。脚本：`organize.py`

```bash
python organize.py <导出目录>                          # 只出报告，不动文件
python organize.py <导出目录> --out <vault> --apply    # 确认后真正整理
```

### 执行原则

1. **默认只体检不写盘**——先看报告，用户确认后再 `--apply`
2. **删除降级为移动**——一律移到 `_trash/`，随时可撤回，绝不真删
3. **优先复用已有分类**——不擅自新建，归不掉的进 `_inbox/` 交人工

### 流水线（顺序即优先级）

```
扫描 → ①敏感隔离 → ②去重 → ③待办归堆 → ④碎片识别 → ⑤根目录归类 → ⑥图片链接修复 → 出报告 → 落盘
```

| 阶段 | 判据 | 去向 |
|---|---|---|
| ① 敏感 | 身份证 / 手机号 / 银行卡 / 邮箱 / 密码密钥 / 详细地址 | `_sensitive/` |
| ② 去重 | 正文去空白去标点后 SHA256 相同，保留字数最多的 | `_trash/` |
| ③ 待办 | 含 `- [ ]` 未完成项 | `_todo/` |
| ④ 碎片 | 正文 < 20 字且无图片 | `_fragments/` |
| ⑤ 归类 | 关键词 + bigram 指纹匹配已有分类 | 已有分类 / `_inbox/` |
| ⑥ 修链 | `![](assets/x.png)` → `![[x.png]]`，图片收拢到 `_assets/` | — |

**优先级：敏感 > 重复 > 待办 > 碎片 > 归类。**

### 可选参数

| 参数 | 作用 |
|---|---|
| `--min-chars N` | 碎片字数阈值，默认 20 |
| `--classify-threshold N` | 归类相似度阈值，默认 0.05，调高更保守 |
| `--link-style wiki\|relative` | 图片链接风格，默认 `![[x.png]]` |
| `--rules rules.json` | 自定义关键词映射和敏感正则 |

> ⚠️ 敏感正则**不能用 `\b`**——中文也是 `\w` 字符，"手机号138..." 中间没有 `\b` 边界，会漏判。已改用 `(?<!\d)` / `(?!\d)` 数字边界。

---

## Agent 行为约束

- 用户要导出便签时，**主动推荐自动续跑器**，不要让他傻等或手动一遍遍重跑
- 见到"大量导出失败"时，**先怀疑超时失效**，不要跟着误判成数据丢失
- 执行 `--apply` 前，**必须**先展示 `report.md` 统计结论并等待确认
- 清空 `_trash/` 前，**必须**逐条列出文件名二次确认，不批量静默删除
- `_sensitive/` 只是**疑似**，不要替用户判定"这个不用管"
- 用户说"删掉"时，先确认是移到 `_trash/` 还是真删——默认只移动
- 用户说"我删了/挪了/改了某些文件"时，**当作正常操作接受**：不要把之后的文件缺失当异常反复排查，更**禁止**从 checkpoint、会话历史或备份里擅自把文件拉回来
- 自己发现 `output/` 内文件与预期不符（缺失、改名、内容变动）时，**先问一句"这是你手动改的吗"**，得到确认前不要擅自恢复或重新导出覆盖
